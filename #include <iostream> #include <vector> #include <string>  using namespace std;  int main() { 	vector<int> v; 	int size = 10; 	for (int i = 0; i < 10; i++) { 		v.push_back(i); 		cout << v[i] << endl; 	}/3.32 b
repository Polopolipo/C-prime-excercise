#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main()
{
	vector<int> v;
	int size = 10;
	for (int i = 0; i < 10; i++) {
		v.push_back(i);
		cout << v[i] << endl;
	}

	// Copying v into d
	vector<int> d = v;
	for (auto x : d)
		cout << x << endl;
}
