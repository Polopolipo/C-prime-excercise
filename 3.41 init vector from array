#include <iostream>
#include <vector>
#include <string>


using namespace std;

int main()
{
	int arr[] = { 1,1,1,2,3,4 };
	vector<int> v(begin(arr), end(arr));
	for (auto x : v)
		cout << x << endl;
}
